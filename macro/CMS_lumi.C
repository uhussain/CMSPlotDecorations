#include "TPython.h"
#include "TPad.h"
#include "TString.h"

int CMS_lumi(TPad* pad, int iPeriod, int iPosX) {
  // TODO
  // TString statement = "dotroot.CMSPlotDecorations.CMSlumi(%s, %d, %d)";
  // statement.sprintf(...);
  // int returnValue = TPython::Eval(statement);
  // return returnValue;
  return -1;
}

